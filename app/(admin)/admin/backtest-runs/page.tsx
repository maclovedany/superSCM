import PageHeader from '@/components/shell/page-header';import Panel from '@/components/ui/panel';import BacktestRunTrigger from '@/components/admin/backtest-run-trigger';import {getForecastRuns,getModelComparison} from '@/lib/scm';
export const dynamic='force-dynamic';export default async function BacktestRunsPage(){const[forecast,data]=await Promise.all([getForecastRuns(),getModelComparison()]);return <section className="analysis-page"><PageHeader eyebrow="ADMIN" title="Backtest Runs" description="저장된 Forecast와 검증 Actual을 비교합니다."/><Panel title="새 Backtest" description="Forecast 재실행 없이 scoring만 수행합니다.">{forecast.error?<p className="text-danger">{forecast.error}</p>:<BacktestRunTrigger forecastRuns={forecast.rows.filter(r=>r.status==='SUCCESS')}/>}</Panel><Panel title="Backtest 이력">{data.error?<p className="text-danger">{data.error}</p>:<div className="analysis-table-wrap"><table className="analysis-table"><thead><tr><th>ID</th><th>Forecast Run</th><th>상태</th><th>Metric</th></tr></thead><tbody>{data.runs.map(r=><tr key={String(r.backtest_run_id)}><td>{String(r.backtest_run_id)}</td><td>{String(r.forecast_run_id)}</td><td>{String(r.status)}</td><td>{String(r.metric)}</td></tr>)}</tbody></table></div>}</Panel></section>}
