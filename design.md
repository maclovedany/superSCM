# SuperSCM 디자인 시스템

> 이 문서가 화면 디자인의 **유일한 기준**입니다.
> 색·글꼴·간격을 화면 파일에 직접 쓰지 않습니다. 여기 정의된 토큰만 씁니다.
>
> 확정: 2026-08-27 (v2 · 라이트 테마 전환) · 함께 읽을 문서 — `AGENTS.md` · `step.md` · `renew.prd`

---

## 1. 방향

**밝고 산뜻한 데이터 콘솔 — 뉴모피즘(Neumorphism).**

숫자가 크고, 여백이 넉넉하고, 카드가 배경에서 **부드럽게 솟아오른** 화면입니다.
카드와 배경이 같은 색이며, 층은 선이 아니라 **빛과 그림자**가 만듭니다. 색은 데이터에만 씁니다.

뉴모피즘을 고른 이유는 이 제품이 **온종일 들여다보는 관제 화면**이기 때문입니다.
경계선이 서른 개씩 그어진 화면은 오래 보면 눈이 지칩니다. 선을 지우고 면의 높낮이로 구분하면
정보 밀도를 지키면서도 화면이 조용해집니다.

### v1(다크) 대비 무엇이 바뀌었나

| | v1 | v2 (현재) |
|---|---|---|
| 테마 | 다크 관제 콘솔 | **라이트 · 밝은 회색 캔버스 + 흰 카드** |
| 모서리 | 10~14px | **16~20px · 넉넉하게** |
| 층 구분 | 명도 차 + 경계선 | **두 광원의 그림자(뉴모피즘) · 선 거의 없음** |
| 수치 굵기 | 700 (굵게) | **500 (중간) · 크게, 자간 좁게** |
| 라벨 | mono 대문자 | **일반 문장 대소문자 · 회색** |
| 활성 메뉴 | 초록 좌측 바 | **잉크 블랙 알약(pill)** |
| 로고 마크 | 파란 사각 아이콘 | **없음. 워드마크만** |

**폐기** — v1 다크 토큰 전체. 변수 **이름은 유지**하고 값만 바꿨으므로, 화면 코드는 고칠 필요가 없습니다.

---

## 2. 원칙

**① 숫자가 주인공이다**
KPI 수치는 크고, 굵기는 500이며, 자간을 좁힙니다. 굵게 만들지 않습니다. 크기만으로 충분합니다.

**② 색은 데이터에만 쓴다**
회색과 흰색이 화면의 90%입니다. 파랑·초록·분홍은 차트와 상태에만 나타납니다. 배경을 색으로 칠하지 않습니다.

**③ 선보다 여백**
경계선을 긋기 전에 여백을 먼저 씁니다. 카드는 아주 옅은 그림자로 떠 있고, 표 안에서만 선을 씁니다.

**④ 계산 불가는 계산 불가처럼 보여야 한다** ★
`renew.prd` 20.2 와 `AGENTS.md` 규칙 5. 값이 없으면 **숫자 자리에 숫자를 넣지 않습니다.** §8.2 참조.

**⑤ 코드는 코드처럼 보인다**
`ITEM012` · `B-4092` · `CONDITIONALLY_AVAILABLE` 은 사람이 대조하는 식별자입니다. 고정폭으로 씁니다. **라벨에는 고정폭을 쓰지 않습니다.**

**⑥ AI 는 옆에 선다**
`renew.prd` 31.4 — LLM 은 부가 계층입니다. AI 패널은 우측 레일이나 배너에 두고, 사라져도 화면이 성립해야 합니다.

---

## 3. 색 토큰

### 3.1 중립 — 화면의 90%

**배경과 면은 모두 흰색입니다.** 층은 그림자가 만듭니다.
`--surface-3` 은 "눌린 면" 이며 입력창에 씁니다. 배경이 순백이므로 눌린 면은 **어두워야** 눌린 것으로 보입니다.

예외는 **요약(KPI) 카드 하나**입니다. 이 카드만 큰 라운드와 옅은 파랑 그라디언트를 씁니다(§6.4).
화면에서 가장 먼저 읽어야 하는 것이 요약 숫자이기 때문입니다. 나머지 면은 조용히 흰색으로 둡니다.

```css
--surface-0: #FFFFFF;   /* 페이지 캔버스 */
--surface-1: #FFFFFF;   /* 떠 있는 면 — 사이드바 · 레일 · 칩 */
--surface-2: #FFFFFF;   /* 패널 · 표 */
--surface-3: #F1F3F7;   /* 눌린 면 — 입력창 */

/* 두 광원. 왼쪽 위에서 빛이 오고 오른쪽 아래에 그림자가 집니다.
   배경이 순백이라 흰빛 쪽은 거의 드러나지 않고 그림자 쪽이 층을 만듭니다.
   그만큼 그림자를 부드럽게 잡았습니다. */
--nm-light:     #FFFFFF;
--nm-dark:      rgba(148, 163, 184, .34);
--nm-dark-soft: rgba(148, 163, 184, .20);

--border:        #E8EBF0;   /* 표 구분선에만 남깁니다 */
--border-strong: #CFD5E0;

--ink:       #1A1D24;   /* 잉크 — 활성 알약 */
--ink-hover: #262A33;

--text-1: #1A1D24;   /* 제목 · 수치 */
--text-2: #4B5162;   /* 본문 */
--text-3: #79808F;   /* 라벨 · 축 · 보조 */
--text-on-accent: #FFFFFF;
```

배경이 순백이 아니게 되어 `--text-3` 을 한 단계 진하게 잡았습니다. 대비를 지키기 위해서입니다.

### 3.2 강조 — 데이터에만

```css
--primary:       #2563EB;              /* 파랑 — 행동 · 링크 · 포커스 */
--primary-hover: #1D4ED8;
--primary-soft:  rgba(37, 99, 235, .08);

--secondary: #16A34A;   /* 초록 */
--tertiary:  #F59E0B;   /* 앰버 */
--danger:    #DC2626;   /* 빨강 */
--danger-hover: #B91C1C;
```

### 3.3 상태

라이트 테마에서는 **옅은 배경 + 진한 글자**를 씁니다. 테두리는 쓰지 않습니다.

```css
/* SAFE */
--safe: #16A34A;  --safe-fg: #15803D;  --safe-bg: #DCFCE7;  --safe-border: transparent;

/* WARNING */
--warn: #F59E0B;  --warn-fg: #B45309;  --warn-bg: #FEF3C7;  --warn-border: transparent;

/* CRITICAL */
--crit: #DC2626;  --crit-fg: #B91C1C;  --crit-bg: #FEE2E2;  --crit-border: transparent;

/* UNKNOWN / 계산 불가 — 색을 쓰지 않는 것이 의도입니다 */
--unknown-fg: #71717A;  --unknown-bg: #F4F4F5;  --unknown-border: transparent;

/* INFO */
--info: #2563EB;  --info-fg: #1D4ED8;  --info-bg: #DBEAFE;  --info-border: transparent;
```

**UNKNOWN 에 색을 주지 않습니다.** 계산 불가가 초록·노랑·빨강 중 하나로 보이면 판단을 오염시킵니다.

### 3.4 그림자 — 뉴모피즘의 두 겹

**항상 두 겹을 함께 씁니다.** 한 겹만 쓰면 평평해 보이고 뉴모피즘이 아니게 됩니다.

```css
/* 솟아오른 면 */
--shadow-sm:   -5px -5px 12px var(--nm-light),  5px  5px 12px var(--nm-dark-soft);
--shadow-md:   -9px -9px 22px var(--nm-light),  9px  9px 22px var(--nm-dark);
--shadow-pill: -2px -2px  5px var(--nm-light),  2px  2px  5px var(--nm-dark-soft);

/* 눌린 면 */
--shadow-inset:    inset -3px -3px 7px var(--nm-light), inset 3px 3px 7px var(--nm-dark);
--shadow-inset-sm: inset -2px -2px 4px var(--nm-light), inset 2px 2px 4px var(--nm-dark-soft);
```

| 대상 | 그림자 |
|---|---|
| 패널 · 레일 | `--shadow-sm` |
| **요약(KPI) 카드** | `--kpi-rim` + `--kpi-shadow` (§6.4) |
| 칩 · 배지 · 보조 버튼 | `--shadow-pill` |
| 입력창 · 검색창 · 드롭존 | `--shadow-inset-sm` |
| **선택된(필터 걸린) 카드** | `--shadow-inset` + `--surface-3` |
| 카드 hover | 더 큰 두 겹 + 네온 (§6.4) |

**빛의 방향을 뒤집지 않습니다.** 화면 전체에서 빛은 왼쪽 위에서만 옵니다.

배경이 흰색이라 위쪽 흰빛은 눈에 띄지 않습니다. 그래도 토큰에 남겨 둡니다.
상태 배경(초록·앰버 카드) 위에서는 흰빛이 실제로 보이고, 배경 밝기를 조정할 때 그대로 살아나기 때문입니다.

### 3.5 모서리 · 간격 · 레이아웃

```css
--r-xs: 6px;  --r-sm: 8px;  --r-md: 12px;
--r-lg: 16px; --r-xl: 20px; --r-pill: 999px;

--s-1: 4px;  --s-2: 8px;  --s-3: 12px; --s-4: 16px;
--s-5: 20px; --s-6: 24px; --s-8: 32px; --s-10: 40px;

--sidebar-w: 232px;  --rail-w: 320px;  --topbar-h: 60px;
```

카드는 `--r-lg`(16px), 알약과 배지는 `--r-pill`, 입력과 버튼은 `--r-md`.

---

## 4. 타이포그래피

### 4.1 글꼴

| 역할 | 글꼴 |
|---|---|
| 표제 · 본문 · **수치** | **Pretendard**(한글) + **Inter**(영문·숫자) |
| 코드 · 식별자 | **JetBrains Mono** |

Inter 에는 한글 글리프가 없으므로 Pretendard 를 앞에 둡니다. 두 글꼴은 메트릭이 유사합니다.

**고정폭은 식별자에만 씁니다.** v1 에서 라벨·표 헤더에 쓰던 mono 대문자는 폐기했습니다.

### 4.2 스케일

| 이름 | 크기 / 행간 | 굵기 | 자간 | 용도 |
|---|---|---|---|---|
| `display` | 48 / 52 | **500** | -0.03em | KPI 대형 수치 |
| `h1` | 36 / 42 | 500 | -0.02em | 페이지 제목 |
| `h2` | 19 / 26 | 600 | -0.01em | 패널 제목 |
| `h3` | 15 / 22 | 600 | 0 | 카드 제목 |
| `body` | 14 / 22 | 400 | 0 | 본문 |
| `sm` | 13 / 20 | 400 | 0 | 보조 설명 |
| `label` | 13 / 18 | 400 | 0 | **회색 라벨. 대문자로 만들지 않습니다** |
| `code` | 12.5 / 18 | 500 | 0 | mono · 식별자 |

수치 굵기가 500인 것이 핵심입니다. 700으로 만들면 무거워지고 이 디자인이 아니게 됩니다.

### 4.3 숫자

```css
.num { font-variant-numeric: tabular-nums; }
```

- **KPI 대형 수치** — 본문 글꼴 + `tabular-nums`, 500, 자간 -0.03em
- **표 안 숫자** — 본문 글꼴 + `tabular-nums` + 우측 정렬 (mono 를 쓰지 않습니다)
- 단위는 수치보다 작고 흐리게: `14,280` + `개`
- 증감은 부호 필수: `+1.2%` · `−5일`

### 4.4 고정폭을 쓰는 곳

식별자와 enum 뿐입니다.

```
ITEM012   RUN-2026-08-27-01   B-4092   CONDITIONALLY_AVAILABLE   ⌘K
```

---

## 5. 레이아웃

### 5.1 셸

```
┌────────────┬──────────────────────────────────────────────────┐
│ SuperSCM   │  [🔍 검색        ]        AI Agent  알림  홍길동 │ 60px
│ Strategic  ├──────────────────────────────────────────────────┤
│            │                                                  │
│ ▪ 대시보드  │  발주 추천                            [내보내기]  │
│   수요예측  │  전 세계 공급망 현황과 발주 권고                  │
│   모델비교  │                                                  │
│            │  ┌────────┐┌────────┐┌────────┐┌────────┐        │
│ 재고·공급   │  │ 14,280 ││  5,500 ││ Oct 15 ││   28일 │        │
│   재고전개  │  │ 현재고  ││ 입고예정││ 결품예상││ 소진   │        │
│   소진위험  │  └────────┘└────────┘└────────┘└────────┘        │
│   리드타임  │  ┌──────────────────────────┐┌──────────────┐    │
│            │  │                          ││ ✦ AI Insight │    │
│            │  │   주 패널 (표 · 차트)     ││              │    │
│ [S&OP 실행] │  │                          ││  긴급 알림    │    │
│ 지원 · 상태 │  └──────────────────────────┘└──────────────┘    │
└────────────┴──────────────────────────────────────────────────┘
    232px                              1fr            320px
```

- 캔버스는 `--surface-0`(#F4F4F5), 카드는 흰색
- 콘텐츠 패딩 `--s-8`(32px), 그리드 간격 `--s-4`(16px)
- 사이드바에 **로고 마크를 쓰지 않습니다.** 워드마크(`SuperSCM`)만 둡니다

### 5.2 반응형

| 폭 | 동작 |
|---|---|
| ≥ 1440px | 기준 |
| 1160–1439 | 우측 레일 280px |
| 900–1159 | 레일이 주 패널 아래로. KPI 2열 |
| < 900 | 사이드바 아이콘 전용(68px). KPI 1열. 표는 가로 스크롤 |

표는 줄바꿈하지 않고 `overflow-x: auto` 로 스크롤합니다.

---

## 6. 컴포넌트

### 6.1 사이드바

```
SuperSCM                      19px / 600 / --text-1   ← 로고 마크 없음
Strategic Planner             12px / --text-3

▪ 대시보드                     활성 = 잉크 블랙 알약 + 흰 글자
  수요 예측                    기본 = --text-2
  모델 비교          예정      hover = 옅은 노란 배경 (--nav-hover-bg)

[ S&OP 실행 ]                 전폭 primary 버튼
지원 · 시스템 정상 ●
```

**활성 메뉴는 잉크 블랙 알약**입니다. 이 시스템의 시그니처입니다(§9). 좌측 바를 쓰지 않습니다.

**hover 는 옅은 노란 배경**(`--nav-hover-bg` `#FDF6E0`)으로 알립니다. 그림자로 띄우지 않습니다.
메뉴는 세로로 촘촘히 붙어 있어서, 항목마다 그림자가 생기면 활성 알약과 층이 겹쳐 읽기 어려워집니다.
면을 칠하는 편이 명확하고 조용합니다.

이 노랑은 상태색 앰버(`#F59E0B`)보다 **훨씬 옅고 따뜻합니다.** `WARNING` 배지와 헷갈리지 않아야 하기 때문입니다.
메뉴는 데이터가 아니라 화면 껍데기이므로, 여기서만 예외적으로 색을 씁니다(§14-6 참조).

### 6.2 탑바

캔버스와 같은 배경, 아래 경계선 1px. 좌측 검색(알약, `--surface-3`), 우측 칩과 사용자 표시. 아이콘 버튼은 원형입니다.

### 6.3 페이지 헤더

```
발주 추천  [ROLE: ADMIN]                          [내보내기]
전 세계 공급망 현황과 발주 권고
```

제목 `h1`(36/500), 부제 `sm`/`--text-2`, 메타 칩은 고정폭 알약.

### 6.4 KPI 카드

```
┌──────────────────────────────┐
│ 현재고                    📦 │  ← 13px / --text-3, 아이콘 우상단
│                              │
│ 14,280        +1.2%          │  ← 48px / 500 / 자간 -0.03em
│ 개                           │
│ 전 세계 합산                  │  ← 13px / --text-3
└──────────────────────────────┘
```

**요약 카드만 다르게 생겼습니다.** 화면에서 가장 먼저 읽어야 하는 것이라 유일하게 색과 형태를 씁니다.

```css
--kpi-radius: 26px;                                  /* 큰 라운드 */
--kpi-bg: linear-gradient(145deg,
            #FBFDFF 0%, #EEF4FD 55%, #E9F0FC 100%);  /* 옅은 파랑 */
--kpi-shadow: -6px -6px 14px #FFFFFF,
              6px 8px 20px rgba(148,163,184,.26);     /* 살짝 떠 보이는 정도 */
--kpi-rim: inset 1px 1px 0 rgba(255,255,255,.9);      /* 위 모서리 빛 */
```

- **그라디언트 방향은 145deg** — 빛이 왼쪽 위에서 오므로 왼쪽 위가 밝습니다. 그림자 방향과 어긋나면 입체가 무너집니다
- **`--kpi-rim`** — 위쪽 모서리에 얇게 걸리는 흰 선. 소프트 UI 특유의 부드러움은 이 한 줄이 만듭니다
- **그림자는 약하게.** 강하면 카드가 무거워지고 유리처럼 보입니다. 글래스모피즘이 아니라 뉴모피즘입니다
- **위험·주의 변형** — 같은 형태에 빨강·앰버 그라디언트만 바꿉니다 (`--kpi-bg-crit` · `--kpi-bg-warn`)
- 값이 없으면 §8.2 의 null 표기법

**패널 · 레일 · 표에는 이 처리를 쓰지 않습니다.** 흰색에 `--r-lg`, `--shadow-sm` 그대로입니다.
전부 그라디언트로 만들면 요약 카드가 눈에 띄지 않게 됩니다.

#### KPI 카드는 눌러서 목록을 좁힌다 ★

**숫자를 보여줬으면 그 내역도 바로 보여줍니다.**

"위험 3건" 을 보고 나서 그 3건이 무엇인지 알려고 표를 다시 뒤지게 만들지 않습니다. **카드를 누르면 아래 목록이 그 카드의 데이터로 좁혀집니다.** 같은 카드를 다시 누르면 풀립니다.

```
┌──────────────┐  누르면
│ 위험      ⚠ │  ────→   아래 표가 위험 4건만 표시
│ 4 / 20       │          [위험] 만 보는 중 · 4 / 20건   [전체 보기]
│ 필터 적용됨   │
└──────────────┘
```

**규칙**

| | |
|---|---|
| 상태 저장 | **URL 쿼리**(`?filter=critical`). 화면은 서버 컴포넌트로 남고, 링크 공유와 뒤로 가기가 됩니다 |
| 정의 위치 | 화면 상단의 `FILTERS: FilterSpec<T>[]` 배열 **한 곳**. 카드와 목록이 같은 정의를 봅니다 |
| hover 표시 | **네온 시안 테두리 + 들어올림.** 아래 참조 |
| 켜짐 표시 | **카드가 눌려 들어갑니다** (`--shadow-inset` + `--kpi-bg-pressed`). 그라디언트 방향도 뒤집혀 위쪽이 그늘집니다. 뉴모피즘에서 inset 은 "눌린 상태" 를 뜻해 의미와 형태가 맞아떨어집니다 |
| 알림 줄 | 필터가 걸리면 목록 위에 `FilterNotice` 로 "무엇만 · 몇 건" 과 [전체 보기] 를 띄웁니다 |
| 결과 0건 | 표를 비우지 말고 `EmptyState` 로 "해당하는 항목이 없습니다" 를 씁니다 |
| **누르면 안 되는 카드** | 목록으로 좁혀지지 않는 값(합계·기간·평균)에는 `filter` 를 주지 않습니다. 눌러도 아무 일이 없는 카드가 더 나쁩니다 |

합계 카드가 필요하면 **뜻을 바꿔 필터와 맞춥니다.** 예: "적재된 행 12,430" → "적재 완료 3건"(누르면 그 3건).

**hover 효과**

지금 무엇을 가리키고 있는지 분명해야 합니다. 옅은 변화로는 알아채기 어렵습니다.

```css
transform: translateY(-5px) scale(1.02);
box-shadow:
  inset 0 0 0 1.5px var(--neon-strong),   /* 네온 시안 테두리 */
  0 0 0 4px var(--neon-glow),             /* 번짐 */
  0 0 20px var(--neon-glow-wide),         /* 넓은 빛 */
  0 16px 34px rgba(24,24,27,.12);         /* 떠 있는 그림자 */
transition: 260ms cubic-bezier(0.16, 1, 0.3, 1);
```

- 테두리를 `border` 가 아니라 **inset 그림자**로 그립니다. `border` 를 쓰면 레이아웃이 1.5px 밀립니다
- 이징은 끝에서 부드럽게 멎는 곡선(`--ease-lift`)입니다. 선형이면 뚝 멈춰 싸구려로 보입니다
- 누르는 순간에는 살짝 눌립니다 (`translateY(-1px) scale(1.004)`, 90ms)
- **hover 가 상태 변형보다 우선합니다.** 지금 가리키는 곳이 가장 분명해야 합니다
- **선택된 카드는 hover 해도 다시 떠오르지 않습니다.** 눌린 채로 네온만 두릅니다. 떠오르면 "필터가 풀렸나?" 로 읽힙니다
- 키보드 `:focus-visible` 도 같은 모양입니다. 기본 파란 외곽선은 겹치지 않게 뺍니다
- `prefers-reduced-motion` 에서는 크기 변화 없이 테두리로만 알립니다

네온 시안(`--neon` `#22D3EE` · `--neon-strong` `#06B6D4`)은 **누를 수 있는 카드의 hover 에만** 씁니다.
데이터에 쓰지 않습니다. 상태색(초록·앰버·빨강)과 역할이 겹치지 않아야 hover 가 상태 위에서도 읽힙니다.

구현은 `lib/filter.ts` · `components/ui/kpi-card.tsx` · `components/ui/filter-notice.tsx`.

### 6.5 패널 카드

흰 배경 + `--r-lg` + `--shadow-sm`. 헤더는 제목(`h2`) + 우측 컨트롤. 본문 패딩 `--s-6`. 헤더 아래 구분선은 표가 붙을 때만 긋습니다.

### 6.6 배지

```css
.badge {
  font-size: 12px; font-weight: 500;
  padding: 4px 10px; border-radius: var(--r-pill);
}
```

옅은 배경 + 진한 글자. **테두리를 쓰지 않습니다.** 대문자로 만들지 않습니다.

| 클래스 | 표기 |
|---|---|
| `.badge.safe` | 안정 · 정상 |
| `.badge.warn` | 주의 · 지연 |
| `.badge.crit` | 위험 · 결품 |
| `.badge.unknown` | 산출 불가 |
| `.badge.info` | 진행 중 |

색만으로 상태를 표현하지 않습니다. 배지에는 반드시 글자가 들어갑니다.

### 6.7 버튼

| 종류 | 스타일 | 용도 |
|---|---|---|
| Primary | `--primary` 배경 / 흰 글자 | 화면당 1개 — 승인 · 실행 |
| Secondary | 흰 배경 / `--border` 테두리 | 보조 행동 |
| Ghost | 투명 / hover 시 `--surface-3` | 아이콘 버튼 |
| Danger | `--danger` 배경 / 흰 글자 | 삭제 · 거부. 확인 절차 필수 |

높이 36px(기본) / 42px(주요) · `--r-md` · 13px/500. 아이콘 버튼은 **원형**(`--r-pill`).

### 6.8 데이터 테이블

- 헤더 — 13px 회색, **대문자로 만들지 않습니다.** 하단 1px 선
- 행 높이 52px · 행 구분선 `--border` · hover `--surface-3`
- 선택 행 — 배경 `--primary-soft`
- 숫자·날짜는 우측 정렬 + `tabular-nums`. 식별자만 mono

### 6.9 알림 행

좌측 3px 상태 색 막대 + 유형(13px, 상태색, 600) + 본문 + 시각. 알림은 시간 순 목록이라 좌측 막대가 훑기에 유리합니다.

### 6.10 인사이트 배너 (AI)

```
✦ AI INSIGHT
아시아 법인 리드타임 변동(+12일)으로 10월 15일 결품이 예상됩니다.
[특급 시뮬레이션]  [발주 상세]
```

**옅은 파랑–보라 그라디언트 배경**에 `--r-lg`. AI 가 말하는 자리임을 색으로 구분합니다. 본문 안의 핵심 수치는 상태색으로 강조합니다.

이 시스템에서 그라디언트를 쓰는 유일한 곳입니다.

### 6.11 AI 우측 레일

흰 카드, 폭 320px. `✦ AI Insight` 헤더 → 대상 식별자(mono) → 설명 문단(`--surface-3` 박스) → 근거 타일 2×2 → 행동 버튼.

**LLM 응답이 없어도 이 레일만 비고 나머지 화면은 그대로 동작해야 합니다.**

### 6.12 업로드 · 검증 (관리자)

- 드롭존 — `1.5px dashed var(--border-strong)` · `--r-lg` · hover 시 `--primary`
- 검증 오류 카드 — `행 47` + 심각도 배지 + 사유 + **원본 행을 mono 코드 블록**(`--surface-3`)
- 하단 — `오류 목록 내려받기`(Secondary) · `배치 되돌리기`(Danger)

---

## 7. 차트

`recharts@3.10.1` 을 쓰며 **`components/chart/` 만 import 합니다**(`step.md` §4.1).

### 7.1 토큰

```css
--chart-grid: #E4E4E7;      /* 가로선만. 세로선은 긋지 않습니다 */
--chart-axis: #A1A1AA;
--chart-actual: #18181B;    /* 실적 = 잉크 블랙 굵은 실선 */
```

### 7.2 시리즈 색 (순서 고정)

```ts
export const SERIES_COLORS = ['#2563EB','#16A34A','#EC4899','#F59E0B','#8B5CF6','#06B6D4'];
```

`lib/chart-colors.ts` 에서 모델 ID → 색을 고정 매핑합니다. 화면마다 같은 모델이 다른 색이면 안 됩니다.

### 7.3 표현 규칙

| 의미 | 표현 |
|---|---|
| 실적 (Actual) | **잉크 블랙 2.5px 실선** — 항상 가장 진하게 |
| 모델 예측 | 시리즈 색 2px 실선 |
| 미래 예측 구간 | 같은 색 파선 `4 4` |
| 검증 구간 음영 | `ReferenceArea` · `rgba(37,99,235,.06)` |
| 예측구간 P80 / P90 | `Area` · 시리즈 색 알파 `.12` / `.06` |
| 결품(0 이하) 영역 | `rgba(220,38,38,.10)` + 0선 `--crit` 파선 |
| 면적 채움 | 시리즈 색 → 투명 세로 그라디언트 |

**범례는 차트 위 알약 칩**으로 두고 클릭으로 토글합니다. 토글은 재조회 없이 즉시 반영합니다(`renew.prd` 16.5).

### 7.4 금지

3D · 원형 그래프 · 6개 초과 동시 시리즈 · 색만으로 실적/예측 구분(선 종류로도 구분).

---

## 8. 상태 표현 ★

### 8.1 4상태

| 상태 | 배지 | 뜻 |
|---|---|---|
| `SAFE` | 안정 (초록) | 여유 있음 |
| `WARNING` | 주의 (앰버) | 리드타임 이내 결품 예상 · **발주 가능** |
| `CRITICAL` | 위험 (빨강) | 지금 발주해도 결품 후 도착 |
| `CALCULATION_UNAVAILABLE` | 산출 불가 (회색) | 판단 불가 |

### 8.2 계산 불가 표기법 — null 표기 ★

**규칙: 숫자 자리에 숫자를 넣지 않는다.**

```
┌──────────────────────────────┐
│ 결품 예상일               📅 │
│                              │
│ —                            │  ← em dash, --text-3, display 크기 유지
│ 리드타임 미확정               │  ← 12px, --unknown-fg
└──────────────────────────────┘
```

- 표시 문자는 **`—`(em dash)** 하나로 통일. `0` · `-` · `N/A` · `999` 금지
- **사유를 반드시 함께 표시** — `NO_USAGE_HISTORY` · `NO_LEADTIME` · `NO_INVENTORY_DATA` · `INSUFFICIENT_SAMPLE`
- 정렬 시 **맨 뒤로**. 0 으로 취급해 앞에 두지 않습니다
- 집계에서 제외하고, 제외했음을 밝힙니다

### 8.3 신뢰도

표본이 부족한 리드타임 통계에는 값 옆에 붙입니다(`renew.prd` 18.2). `HIGH` 는 생략하고 `MEDIUM`·`LOW` 만 표시합니다.

### 8.4 데이터 신선도

`renew.prd` 8.6 · 31.5. 원본이 갱신되면 상단에 띠를 띄웁니다.

```
⚠ 기준 데이터가 2026-08-27 이후 변경되었습니다. 예측 재실행이 필요합니다.  [재실행]
```

배경 `--warn-bg`, 좌측 3px `--tertiary`.

---

## 9. 시그니처

**① 잉크 알약 (Ink Pill)**
활성 메뉴는 잉크 블랙 알약입니다. 화면 어디서든 "지금 여기"가 한눈에 보입니다. 색이 아니라 **명도**로 표시하므로, 상태색(초록·앰버·빨강)과 절대 헷갈리지 않습니다. 상태색을 데이터에만 남겨 두기 위한 선택입니다.

**② null 표기법**
`—` + 사유. 이 제품이 다른 대시보드와 다른 지점이고, `renew.prd` 가 명시적으로 요구한 유일한 시각 규칙입니다.

나머지는 조용하게 둡니다.

---

## 10. 접근성

- **포커스** — `outline: 2px solid var(--primary); outline-offset: 2px`. 제거 금지
- **대비** — `--text-2`(#52525B)는 흰 배경에서 7:1 이상. `--text-3`은 13px 이하 라벨에만
- **색 의존 금지** — 배지에 항상 글자. 차트는 선 종류로도 구분
- **표** — `<th scope>` 와 `caption`, 정렬 상태는 `aria-sort`
- **키보드** — 표 행 선택 · 필터 · 모달 닫기(Esc)

---

## 11. 모션

| 대상 | 시간 |
|---|---|
| hover · 포커스 | 120ms |
| 패널 · 드로어 진입 | 180ms `--ease` |
| 값 갱신 강조 | 배경 400ms 페이드 1회 |

```css
@media (prefers-reduced-motion: reduce) { /* 전부 끕니다 */ }
```

**금지** — 무한 애니메이션, 시차 스크롤, 숫자 카운트업.

---

## 12. 화면 문구

`AGENTS.md` 규칙 7 대로 **한국어**로 씁니다.

- **행동은 결과를 말합니다** — `제출` ✕ / `발주 승인` ○. 흐름 끝까지 같은 이름
- **시스템 용어를 쓰지 않습니다** — `배치 롤백` ✕ / `업로드 되돌리기` ○
- **오류는 사과하지 않고 다음 행동을 알려줍니다**
  - ○ `47행의 품목코드 'ITEM999' 가 마스터에 없습니다. 오류 목록을 내려받아 수정 후 다시 올려주세요.`
- **빈 화면은 초대장입니다** — `데이터 없음` ✕ / `아직 올린 파일이 없습니다. CSV · Excel · JSON 을 올리면 검증부터 시작합니다.` ○
- **조회 실패와 빈 결과를 구분합니다**
- 영어를 남기는 곳은 식별자·지표명뿐 (`WAPE` · `P80` · `ATP` · `MOQ`)

---

## 13. 파일 구조

```
app/globals.css        토큰 · 리셋 · 타입 스케일 · 폰트
styles/shell.css       사이드바 · 탑바 · 페이지 골격 · 반응형
styles/components.css  카드 · 배지 · 버튼 · 표 · 알림 · 폼
styles/chart.css       차트 껍데기 · 범례
lib/chart-colors.ts    시리즈 색 고정 매핑
```

화면별 CSS 파일을 만들지 않습니다.

**순수 CSS 를 유지합니다.** Tailwind 를 넣지 않는 이유는 토큰이 좁고 규칙이 명확해, 의미 있는 클래스명(`.badge.crit`)이 여러 명이 나눠 만들 때 일관성을 강제하기 쉽기 때문입니다.

---

## 14. 금지 사항

1. **hex 색을 화면 파일에 직접 쓰지 않습니다.** `var(--*)` 만
2. **간격은 4px 배수** — `--s-*` 토큰
3. **테두리로 카드를 감싸지 않습니다** — 두 겹 그림자가 층을 만듭니다
4. **그림자를 한 겹만 쓰지 않습니다** — 빛과 어둠이 함께 있어야 솟아 보입니다
5. **빛의 방향을 뒤집지 않습니다** — 화면 전체에서 왼쪽 위에서만 옵니다
6. **상태색을 장식으로 쓰지 않습니다**
7. **계산 불가를 숫자로 채우지 않습니다** (§8.2)
8. **Primary 버튼은 한 화면에 하나**
9. **차트 라이브러리를 화면에서 직접 import 하지 않습니다**
10. **그라디언트는 AI 배너와 차트 면적 채움에만** 씁니다
11. **수치를 700 굵기로 만들지 않습니다** — 500 입니다
12. **라벨과 표 헤더를 대문자·고정폭으로 만들지 않습니다** — 식별자만 고정폭
13. **로고 마크를 넣지 않습니다** — 워드마크만
