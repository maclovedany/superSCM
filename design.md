# SuperSCM 디자인 시스템

> 이 문서는 SuperSCM의 모든 화면에 적용하는 시각·상호작용 기준이다. 새 화면을 만들거나 기존 화면을 개편할 때 이 문서와 `AGENTS.md`, `SCHEMA.md`를 함께 확인한다.

## 1. 제품 인상

SuperSCM은 해외 생산법인 12곳의 발주계획과 공급망 위험을 한 화면에서 판단하는 업무 시스템이다. 화면은 장식적인 대시보드보다 **차분하고 신뢰할 수 있는 운영 콘솔**에 가깝게 만든다.

레퍼런스의 핵심 인상은 다음과 같이 해석한다.

- 밝은 캔버스 위에 흰색 정보 카드를 배치한다.
- 짙은 네이비 사이드바로 업무 영역을 고정한다.
- 블루는 현재 위치와 실행 가능한 행동을 나타낸다.
- 얇은 테두리와 낮은 그림자로 정보 위계를 만든다.
- KPI와 표를 먼저 보여주고, 시각 장식은 판단을 도울 때만 사용한다.
- 강조색은 제한적으로 사용해 숫자의 의미가 먼저 읽히게 한다.

## 2. 디자인 원칙

### 업무 판단 우선

페이지 상단에서 사용자가 알아야 하는 상태, 위험, 다음 행동을 먼저 보여준다. 제목·설명·필터·핵심 지표·상세 표의 순서를 유지한다.

### 한 화면 한 목적

한 페이지는 하나의 질문에 답한다. 예를 들어 리드타임 화면은 “어느 공급처의 실제 소요일이 계획보다 긴가?”에 집중한다. 관련 없는 KPI나 장식용 카드는 추가하지 않는다.

### 데이터 의미를 색으로 보조

색만으로 상태를 전달하지 않는다. 상태 텍스트, 아이콘 또는 보조 문구를 함께 표시한다. 계산 불가 값은 임의의 숫자로 채우지 않고 `—`와 사유를 보여준다.

### 일관된 밀도

카드는 넉넉한 내부 여백을 사용하되, 분석 표는 한 화면에서 충분한 행을 볼 수 있도록 지나치게 크게 만들지 않는다. 같은 종류의 정보는 화면마다 같은 간격·글자 크기·정렬을 사용한다.

### 점진적 강조

기본 정보는 중성색, 현재 선택은 블루, 정상은 그린, 주의는 앰버, 위험은 레드로 표현한다. 한 카드 안에서 강조색은 한 가지를 원칙으로 한다.

## 3. 색상 토큰

기존 `app/globals.css`의 CSS 변수를 기준 토큰으로 사용한다. 새 화면에서 색상 코드를 직접 작성하지 않는다.

### 기본 색상

| 토큰 | 현재 값 | 용도 |
|---|---|---|
| `--canvas` | `#f5f7fb` | 전체 페이지 배경 |
| `--surface` | `#ffffff` | 카드, 표, 입력 영역 |
| `--ink` | `#182230` | 기본 본문과 숫자 |
| `--muted` | `#718096` | 보조 설명, 메타 정보 |
| `--line` | `#e3e8ef` | 카드·표·구분선 테두리 |
| `--navy` | `#17365d` | 사이드바, 강한 제목, 운영 상태 |
| `--blue` | `#2f6fed` | 활성 상태, 주요 버튼, 링크, 포커스 |
| `--pale-blue` | `#edf4ff` | 블루 상태의 옅은 배경 |

### 의미 색상

| 토큰 | 현재 값 | 의미 | 함께 표시할 문구 예 |
|---|---|---|---|
| `--green` | `#1f9d68` | 정상, 완료, 개선 | 정상, 완료 |
| `--pale-green` | `#eaf8f1` | 정상 상태 배경 | 안정 |
| `--amber` | `#bd7211` | 주의, 표본 부족, 검토 필요 | 주의, 확인 필요 |
| `--pale-amber` | `#fff6e4` | 주의 상태 배경 | 표본 부족 |
| `--red` | `#c2413d` | 위험, 실패, 계획 초과 | 위험, 조회 실패 |
| `--pale-red` | `#fff0ef` | 위험 상태 배경 | 긴급 |

레퍼런스의 선명한 블루와 밝은 표면감은 현재 토큰의 `--blue`, `--pale-blue`, `--canvas`, `--surface` 조합으로 구현한다. 레퍼런스에 있는 검정·오렌지 계열을 전역 기본색으로 추가하지 않는다. 의미 없는 장식색이 필요하면 먼저 기존 의미 색상으로 대체할 수 있는지 검토한다.

## 4. 타이포그래피

기본 글꼴은 `Noto Sans KR`이다. 한국어 업무 화면에서 글자 형태와 줄바꿈이 안정적이므로 새 폰트를 추가하지 않는다.

| 역할 | 권장 크기 | 굵기 | 사용처 |
|---|---:|---:|---|
| 페이지 제목 | 25px | 700~800 | `.page-heading h2` |
| 카드 제목 | 15~17px | 700 | `.card-title h3`, 섹션 제목 |
| 핵심 숫자 | 28~34px | 800 | `.metric-value` |
| 본문 | 13~14px | 400~500 | 설명, 표 본문 |
| 보조 문구 | 11~12px | 400~600 | 단위, 메타, 풋터 |
| 라벨 | 10~11px | 700~800 | `.eyebrow`, `.metric-label`, 상태 라벨 |

규칙:

- 제목은 `letter-spacing: -0.5px`에서 `-1px` 범위로 조정한다.
- 라벨과 eyebrow는 대문자보다 한국어 짧은 명사를 우선한다. 영문 시스템 용어가 필요할 때만 자간을 넓힌다.
- 숫자는 우측 정렬하고 단위는 숫자와 시각적으로 분리한다.
- 문장형 설명은 `--muted`를 사용하며, 제목보다 한 단계 낮은 대비를 유지한다.
- 색상·굵기·크기 세 가지를 동시에 강조하지 않는다.

## 5. 레이아웃

### 전체 구조

```text
.app-shell
├── .sidebar       고정 네비게이션, 250px
└── .main
    ├── .topbar     75px 헤더
    └── .content    최대 1440px 콘텐츠 영역
```

- 데스크톱 사이드바 폭은 `250px`로 유지한다.
- 콘텐츠 최대 폭은 `1440px`, 좌우 패딩은 기본 `42px`로 한다.
- 카드 사이 간격은 `20px`, 카드 내부 패딩은 기본 `24px`로 한다.
- 모바일에서는 사이드바를 접고 콘텐츠 좌우 패딩을 `16px`까지 줄인다.
- 화면 전체의 기본 배경은 `--canvas`, 정보가 놓이는 영역은 `--surface`다.

### 그리드

기존 `.grid`, `.grid-2`, `.grid-3`, `.grid-4`를 우선 사용한다.

- KPI 3개: `.grid grid-3`
- 두 영역 비교: `.grid grid-2`
- 네 개의 짧은 지표: `.grid grid-4`
- 표처럼 가로 폭이 중요한 콘텐츠는 임의의 다단 분할보다 `.section card` 전체 폭을 우선한다.
- 모바일에서는 모든 그리드가 한 열로 내려가도 정보 순서가 자연스러워야 한다.

## 6. 내비게이션과 헤더

### 사이드바

- 배경은 `#102b4b` 또는 기존 `--navy` 계열의 짙은 네이비를 사용한다.
- 브랜드는 `.brand`, `.brand-mark`, `.brand-copy` 구조를 재사용한다.
- 활성 메뉴는 블루 배경과 흰색 글자로 표시한다. 활성 메뉴에만 강한 그림자를 허용한다.
- 완료된 워크플로 스텝은 그린, 미완료 스텝은 중성색으로 구분한다.
- 아이콘만으로 메뉴를 만들지 말고 텍스트 레이블을 함께 제공한다.
- 사이드바 하단에는 현재 시스템 상태나 짧은 도움말만 배치한다.

### 상단바

- `.topbar`는 흰색 반투명 배경, 하단 1px 구분선, `75px` 높이를 사용한다.
- 왼쪽에는 현재 업무 영역, 오른쪽에는 데이터 상태·사용자·보조 행동을 둔다.
- 주요 행동은 한 번에 하나만 블루 버튼으로 노출한다.
- 실시간 동기화나 기준일은 `.local-badge`처럼 짧은 상태 배지로 표시한다.
- 스크롤 시 상단바를 고정해 현재 맥락과 주요 행동이 사라지지 않게 한다.

## 7. 페이지 구성

분석 화면은 `app/analysis/leadtime/page.tsx`의 흐름을 본보기로 삼는다.

```text
페이지 제목 + 설명
→ 핵심 KPI 카드
→ 상세 분석 카드
→ 표 또는 보조 설명
```

페이지 제목에는 사용자의 질문을 답하는 명사형 제목을 쓴다. 설명은 어떤 기준으로 데이터를 읽는지 한 문장으로 쓴다.

```tsx
<AnalysisFrame
  title="리드타임 격차"
  description="마스터 리드타임과 실제 실적 P80을 비교해 계획이 짧게 잡힌 공급처를 찾습니다."
>
```

KPI 카드에는 다음 세 요소를 유지한다.

- `.metric-label`: 무엇을 세는지
- `.metric-value`: 숫자 또는 짧은 결과
- `.metric-foot`: 기준·단위·판정 조건

KPI에서 숫자를 계산하지 않는다. 화면은 `analytics` 뷰가 반환한 값을 표현만 한다.

## 8. 카드와 패널

기본 카드는 `.card`를 사용한다.

- 배경 `--surface`
- 테두리 `1px solid var(--line)`
- 모서리 반경 `14px`
- 낮은 그림자 또는 그림자 없음
- 기본 내부 여백 `22~24px`

카드 안에서 제목과 보조 정보를 나란히 둘 때 `.card-title`을 사용한다. 긴 표나 분석 결과는 `.section card`로 감싼다.

레퍼런스의 유리 효과는 장식이 아니라 표면 위계를 위한 것이다. `backdrop-filter`를 새 화면의 기본 카드 효과로 사용하지 않고, 필요한 경우에도 불투명한 흰색 배경과 테두리를 먼저 보장한다.

### KPI 카드

- 숫자는 가장 큰 요소로 두고, 단위는 한 단계 작게 둔다.
- 위험·주의 숫자만 의미 색상을 사용한다.
- 값이 `null`이면 `—`를 보여주고, 풋터에 계산 불가 사유를 적는다.
- 카드 전체가 클릭 가능할 때만 `.metric-interactive`를 사용하고, hover만으로 클릭 가능성을 만들지 않는다.

## 9. 버튼, 입력, 필터

### 버튼

기존 `.button`과 `.button.primary`, `.button.ghost`를 사용한다.

- primary: 저장, 확정, 분석 실행처럼 페이지의 대표 행동
- 기본 버튼: 보조 행동
- ghost: 필터 초기화, 취소, 보조 링크
- 버튼 문구는 동작으로 시작한다: `저장`, `확정`, `다시 조회`
- 아이콘 단독 버튼은 툴팁 또는 `aria-label`을 제공한다.
- 비활성 상태는 색만 낮추지 말고 `disabled` 상태와 이유를 함께 고려한다.

### 입력과 필터

- 입력 높이와 테두리는 기존 `.form-input`, `.table-input`, `.table-select`를 따른다.
- 필터는 표 위에 배치하고 현재 적용 상태를 요약한다.
- 선택값이 많으면 작은 tag를 사용하되, 화면을 가리는 모달식 필터는 피한다.
- 포커스 링은 `--blue`로 분명하게 표시한다.

## 10. 표와 숫자 표시

분석 표는 `.analysis-table-wrap`, `.analysis-table` 또는 기존 `.table-wrap`, `table` 스타일을 재사용한다.

- 헤더는 `--muted`와 11~12px 굵은 글자로 구분한다.
- 텍스트는 좌측, 숫자·날짜·비율은 우측 정렬한다.
- 단위는 모든 행에 반복해도 되지만, 열 제목에서 이미 명확하면 셀에서는 생략할 수 있다.
- 행 hover는 선택 상태처럼 보이지 않는 옅은 배경만 사용한다.
- 표가 가로로 길어지면 컨테이너가 가로 스크롤하고, 열을 임의로 숨기지 않는다.
- 정렬·페이지네이션·필터의 상태가 있다면 현재 상태를 시각적으로 표시한다.

### 상태 tag

기존 `.tag`, `.tag.blue`, `.tag.green`, `.tag.amber`, `.tag.red`, `.tag.gray`를 사용한다.

| 상태 | 클래스 | 예 |
|---|---|---|
| 정상·완료 | `.tag.green` | `SAFE`, `완료` |
| 주의·검토 | `.tag.amber` | `LOW`, `표본 부족` |
| 위험·실패 | `.tag.red` | `CRITICAL`, `조회 실패` |
| 일반·미정 | `.tag.gray` | `UNKNOWN`, `미정` |
| 현재 선택·정보 | `.tag.blue` | `선택됨`, `분석 중` |

`SAFE`, `CRITICAL` 같은 원본 상태값은 화면 문구로 그대로 노출하지 말고 한국어 레이블을 함께 제공한다. 예: `CRITICAL · 위험`.

## 11. 로딩, 오류, 빈 결과

상태는 서로 다르게 표현한다.

### 조회 오류

데이터베이스·권한·스키마 문제로 조회하지 못한 경우는 위험 톤으로 표시한다.

```tsx
<p className="text-danger">조회에 실패했습니다.</p>
<p className="muted">{error}</p>
```

### 빈 결과

조회는 성공했지만 행이 없는 경우는 중성 안내로 표시한다.

```tsx
<p className="muted">표시할 데이터가 없습니다.</p>
```

특히 `analytics` 스키마 미노출로 빈 배열이 될 수 있으므로 오류와 빈 결과를 합치지 않는다.

### 계산 불가

사용 이력이나 리드타임이 없으면 `null`과 사유 코드의 의미를 유지한다.

- `NO_USAGE`: 사용 이력이 없어 소진일을 계산할 수 없음
- `NO_LEADTIME`: 적용할 리드타임이 없어 판단할 수 없음

`999`, `0`, 임의의 추정값을 대신 사용하지 않는다.

## 12. 반응형과 접근성

- 데스크톱 기준은 1280px 이상, 태블릿은 768~1279px, 모바일은 767px 이하로 생각한다.
- 모바일에서는 사이드바를 접고, 표는 가로 스크롤을 허용한다.
- 제목과 설명이 먼저 나오고 KPI·상세 표 순서가 유지되어야 한다.
- 본문 대비는 충분히 유지하고, 회색 텍스트를 작은 크기로 과도하게 사용하지 않는다.
- 모든 폼 요소에는 연결된 레이블이 있어야 한다.
- 키보드 포커스가 보이지 않게 만들지 않는다.
- 색상만으로 위험 여부를 전달하지 않는다.
- 아이콘·이미지에는 의미에 맞는 대체 텍스트 또는 접근 가능한 이름을 제공한다.
- 애니메이션은 짧고 기능을 방해하지 않아야 하며, 상태 전환 자체를 애니메이션에 의존하지 않는다.

## 13. 구현 규칙

새 화면을 만들 때는 다음 순서를 따른다.

1. 데이터 타입과 정규화는 `lib/scm-model.ts`에 둔다.
2. Supabase 조회는 `lib/scm.ts`에 둔다.
3. 화면은 `app/analysis/<기능이름>/page.tsx`에 둔다.
4. 공통 프레임과 표는 `components/analysis/*`를 재사용한다.
5. 스타일은 `app/globals.css`의 기존 클래스와 변수를 우선 사용한다.
6. 부족한 공통 스타일만 `app/globals.css` 끝에 추가한다.

다음은 금지한다.

- Tailwind, styled-components, CSS Modules 등 새 스타일 체계 추가
- 화면 컴포넌트 안에서 평균·분위수·위험 판정 계산
- `raw` 스키마 직접 조회
- 한 화면에서 색상 코드 직접 작성
- 표준 컴포넌트마다 서로 다른 카드 반경·버튼 높이·상태 색상 사용
- 데이터 오류를 빈 결과처럼 표시

## 14. 화면 완료 체크리스트

- [ ] 페이지의 핵심 질문이 제목과 설명만으로 이해된다.
- [ ] 콘텐츠 배경, 카드 표면, 테두리 색이 토큰을 사용한다.
- [ ] 제목·KPI·보조 문구의 위계가 분명하다.
- [ ] 주요 행동 버튼이 한 가지로 명확하다.
- [ ] 숫자 정렬과 단위 표기가 일관된다.
- [ ] 정상·주의·위험·미정 상태가 tag와 텍스트로 함께 표시된다.
- [ ] 조회 오류와 빈 결과를 구분한다.
- [ ] 계산 불가 값을 임의의 숫자로 채우지 않는다.
- [ ] 모바일에서 정보 순서와 표 접근성이 유지된다.
- [ ] 기존 공통 클래스와 분석 컴포넌트를 우선 재사용했다.
- [ ] 변경 후 `npm run build`를 실행했다.
- [ ] 화면을 한 문장으로 설명할 수 있다.

